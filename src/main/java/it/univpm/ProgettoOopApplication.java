package it.univpm;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * <b>Classe</b> contenente il metodo <code>main</code> dell'applicazione.
 */
@SpringBootApplication
public class ProgettoOopApplication {

    /**
     * <b>Metodo</b> <code>main</code> dell'applicazione. Esegue il programma come <code>SpringBootApplication</code>.
     *
     * @param args argomenti inseriti dall'utente in fase di avvio.
     */
    public static void main(String[] args) {
        SpringApplication.run(ProgettoOopApplication.class, args);
    }

}
